import React from 'react';
import { 
    Wrapper 
} from './index.styled';

export const Shipment: React.FC<{url:String}> = ({url}) => {
    return (
        <Wrapper>
       
        </Wrapper>
    )
};